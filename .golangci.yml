linters:
  enable-all: true
  disable:
    - exhaustivestruct
    - goerr113
    - lll
    - wrapcheck
    - wsl


issues:
  exclude-rules:
    # Exclude some linters from running on tests files.
    - path: _test\.go
      linters:
        - gochecknoglobals
        - testpackage
